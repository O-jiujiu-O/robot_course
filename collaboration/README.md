# 跨机器协作目录

每个人交付时复制 `handoff_template/`，重命名为：

```text
collaboration/handoffs/person_X/v版本号_日期/
```

示例：

```text
collaboration/handoffs/person_A/v1.1.0_20260607/
```

填写所有模板文件，并将小型日志放入 `logs/`。大型截图和视频可以单独传输，但必须在 `test-results.md` 中记录文件名和 SHA-256。

